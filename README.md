# Scanly（書類・プリント管理アプリ）／ team-B_section9

家庭や学校から届く紙の書類（プリント）を、スマホで**撮影するだけ**でOpenAIのマルチモーダルAIが「種類・期限」を自動解釈し、アプリ内カレンダーへ自動登録・通知までを行うドキュメント管理アプリです。

---

## 目次

1. [アプリ概要](#1-アプリ概要)
2. [主な機能](#2-主な機能)
3. [画面一覧](#3-画面一覧)
4. [技術スタック・外部サービス](#4-技術スタック外部サービス)
5. [ディレクトリ構成](#5-ディレクトリ構成)
6. [開発環境セットアップ](#6-開発環境セットアップ)
7. [詳細ドキュメント（設計書）](#7-詳細ドキュメント設計書)

---

## 1. アプリ概要

| 項目         | 内容                                                                                     |
| ------------ | ---------------------------------------------------------------------------------------- |
| システム名   | Scanly（書類・プリント管理アプリ）                                                       |
| 目的         | 書類を撮影するだけで「期限・種類」をAIが自動解釈し、カレンダー登録・通知までを自動化する |
| 対象ユーザー | さまざまな書類を日常的に受け取る個人利用のユーザーおよび家庭・世帯                       |
| 開発体制     | 開発チーム4人／開発期間4週間（公開前提）                                                 |

### 解決したい課題

- 書類が届いても後回しにされ積み重なる
- 「いつまでか？」を毎回人間が読み解く必要があり、解釈コストが高い
- 手帳やカレンダーアプリへの手動転記、家族への口頭・LINE連絡などに頼っており、共有漏れや期限忘れが発生する

### 使い方の流れ

1. 新規登録（メールアドレス・表示名）→ 1人でもすぐ使える「個人グループ」が自動作成される
2. 書類が届いたらスマホで撮影
3. AIが期限・種類を自動解析（失敗時は手入力で登録できる）
4. 内容を確認・修正して登録 → カレンダーに反映、家族に通知
5. 提出・支払いが終わったら「済スタンプ」を押す → 以降のリマインドは自動停止

---

## 2. 主な機能

| 機能                 | 概要                                                                                               |
| -------------------- | -------------------------------------------------------------------------------------------------- |
| ログイン/新規登録    | Firebase Authによるメール認証。登録時に個人グループを自動作成。                                    |
| グループ作成・招待   | 家族などのグループを作り、他ユーザーを招待。メンバーは全員同じ権限で操作できる。                   |
| 写真スキャン・AI解析 | 撮影した書類から「期限・種類・カテゴリ」をAIが自動抽出。                                           |
| 解析結果の確認・編集 | AIの抽出結果をその場で修正して登録。解析失敗時は手入力に切り替え。                                 |
| カレンダー表示       | 登録した書類を月表示カレンダーで確認。完了済みはグレーアウト。                                     |
| 済スタンプ           | 完了した書類にスタンプを付けると、以降のリマインドメールを自動キャンセル。                         |
| 期限なし書類の管理   | 期限のない書類もカテゴリ別に保管・検索できる。                                                     |
| 通知・リマインド     | 書類登録時のアプリ内通知（🔔）と、期限前のメールリマインド。メール通知はユーザーごとにON/OFF可能。 |
| カテゴリ管理         | 固定11種類のカテゴリで分類（追加・編集・削除は不可）。                                             |
| 課金・スキャン制限   | 月間の無料スキャン枚数を超えるとStripeの課金導線を表示。毎月1日にリセット。                        |

---

## 3. 画面一覧

| 画面                   | 概要                                                       |
| ---------------------- | ---------------------------------------------------------- |
| ログイン・サインアップ | メール・表示名で認証。登録と同時に個人グループを自動作成。 |
| カレンダー（トップ）   | 月表示カレンダー、書類一覧、撮影ボタン、通知（🔔）。       |
| 撮影・アップロード     | 書類の撮影または画像選択。                                 |
| 解析確認・編集         | AI解析結果の確認・編集、登録先グループの選択。             |
| 書類詳細               | 書類の確認、再編集、済スタンプのON/OFF。                   |
| 設定                   | プラン・スキャン残数、リマインド日数・通知方法の変更。     |
| グループ管理           | グループの招待・メンバー確認。                             |
| 書類一覧               | すべての書類をカテゴリ別に一覧・検索。                     |
| プロフィール編集       | 表示名などの編集。                                         |
| 招待受諾               | 招待メールのリンクから、グループ参加を承諾・辞退。         |
| 新しいグループを作成   | グループの新規作成。                                       |

画面ごとの詳細レイアウトは `docs/画面設計書.md` を参照してください。

---

## 4. 技術スタック・外部サービス

| レイヤー          | 技術                                                                         |
| ----------------- | ---------------------------------------------------------------------------- |
| フロントエンド    | Next.js（App Router）、TypeScript、Tailwind CSS、FullCalendar                |
| バックエンド      | FastAPI（Python）、SQLAlchemy、Alembic                                       |
| 非同期タスク      | Celery（Worker・Beat）                                                       |
| データベース      | PostgreSQL                                                                   |
| キャッシュ/キュー | Redis                                                                        |
| 認証              | Firebase Auth                                                                |
| インフラ          | Docker Compose（開発環境）／Vercel（フロントエンド）・Render（バックエンド） |

| 外部サービス  | 用途                               |
| ------------- | ---------------------------------- |
| OpenAI API    | 書類画像からの期限・種類の自動抽出 |
| SendGrid      | 招待メール・リマインドメールの送信 |
| Stripe        | 有料プランへの課金                 |
| Cloudinary    | 書類画像の保存・配信               |
| Firebase Auth | ユーザー認証                       |

---

## 5. ディレクトリ構成

```
team-B_section9/
├── .github/           # CI/CDワークフロー等
├── backend/           # FastAPI（バックエンド）
├── docs/              # 設計書一式（要件定義・画面設計書・DB設計・API設計）
├── frontend/          # Next.js（フロントエンド）
├── infra/             # インフラ関連設定
├── .env               # 各自のAPIキー等（Git管理対象外）
├── .env.example       # 環境変数テンプレート（新しい変数を追加したら必ず更新）
├── .gitattributes
├── .gitignore
├── compose.yaml       # Docker Compose定義
├── README.md          # 本ドキュメント
└── render.yaml        # Renderへのデプロイ設定
```

`backend/` `frontend/` 内部の詳細構成は、実装が進み次第このセクションに追記していきます。

---

## 6. 開発環境セットアップ

### 必要なもの

- Docker Desktop（最新版）
- Git

### Windowsの方へ：Docker Desktopのメモリ設定

デフォルトのままだとコンテナが起動しないことがあります。
Docker Desktop を開いて Settings → Resources → Memory を **4GB以上** に変更してください。

### 初回セットアップ

```bash
# 1. リポジトリをクローン
git clone ...

# 2. 環境変数ファイルを作成
cp .env.example .env
# .env を開いて、各自のAPIキーを入力してください

# 3. 起動
docker compose up -d
```

### 起動後にアクセスできる画面

| 画面                       | URL                        |
| -------------------------- | -------------------------- |
| フロントエンド             | http://localhost:3000      |
| バックエンドAPI（Swagger） | http://localhost:8000/docs |
| DB確認（Adminer）          | http://localhost:8080      |
| Redis確認                  | http://localhost:8081      |

Adminerのログイン情報：System: PostgreSQL / Server: db / User: appuser / Password: `.env`の`POSTGRES_PASSWORD`の値

### よく使うコマンド

```bash
# 起動
docker compose up -d

# 停止
docker compose down

# ログ確認
docker compose logs -f

# イメージ再ビルド（Dockerfileやrequirements.txtを変更したとき）
docker compose down
docker compose build --no-cache
docker compose up -d

# DBマイグレーション
docker compose exec backend alembic upgrade head
```

### ファイルを変更したとき

| 変更内容         | 必要な対応                                                                  |
| ---------------- | --------------------------------------------------------------------------- |
| Pythonのコード   | 自動でリロードされます（何もしなくてOK）                                    |
| フロントのコード | 自動でリロードされます（何もしなくてOK）                                    |
| requirements.txt | `docker compose build --no-cache` が必要                                    |
| package.json     | `docker compose build --no-cache` が必要                                    |
| compose.yaml     | `docker compose down` してから `docker compose up -d`                       |
| DBのテーブル変更 | チャットで事前告知してから `docker compose down -v && docker compose up -d` |

### DBのテーブルを変更するときのルール

データが全部消えます。必ず事前にチャットで告知してください。

```bash
# データも含めて全リセット（告知してから実行）
docker compose down -v
docker compose up -d
docker compose exec backend alembic upgrade head
```

### .envに追加があったとき

新しい環境変数が追加されたら `.env.example` も必ず更新します。
各自 `.env` に追記してから `docker compose down && docker compose up -d` してください。

---

## 7. 詳細ドキュメント（設計書）

より詳しい仕様は `docs/` フォルダ内の各設計書を参照してください。README側では概要のみを扱い、詳細な仕様変更はまず設計書を更新する運用とします。

| ドキュメント         | 内容                                         | バージョン |
| -------------------- | -------------------------------------------- | ---------- |
| `docs/要件定義.md`   | 機能要件・非機能要件・業務フローなど         | v7.6       |
| `docs/画面設計書.md` | 各画面のレイアウト・画面遷移・API連携        | v2.3       |
| `docs/DB設計.md`     | テーブル定義・ER関係・インデックス           | v1.4       |
| `docs/API設計.md`    | エンドポイント一覧・エラーコード・レート制限 | v2.5       |

---
